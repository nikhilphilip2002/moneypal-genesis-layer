# One-shot backend setup for any module.
#   pip install -r requirements.txt
# Installs the shared engine (editable) plus the per-service web deps.
-e ./packages/genesis_core
fastapi
uvicorn[standard]
openpyxl

