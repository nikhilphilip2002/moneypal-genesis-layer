# One-shot backend setup for any module.
#   pip install -r requirements.txt
# Installs the shared engine (editable) plus the per-service web deps.
-e ./packages/genesis_core
fastapi
uvicorn[standard]
openpyxl
psycopg2-binary
pg8000
sqlalchemy
sqlglot
pyyaml
fastmcp>=4.0.5,<5
mcp>=2.0.0,<3
# Macro ingestion pipeline: crawl (requests/bs4/lxml) + weekly worker (APScheduler).
requests
beautifulsoup4
lxml
APScheduler>=3.10,<4
openai>=1.0,<2
